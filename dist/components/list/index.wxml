<block>
    <view class="list-content" style="{{anonymousState__temp}}">
        <view class="list-wrap">
            <view class="item" bindtap="goToBody" wx:key="{{item.$loopState__temp3}}"
            wx:for="{{loopArray0}}" wx:for-item="item" data-e-goToBody-so="this" data-e-goToBody-a-a="{{item.id}}">
                <image class="img" src="{{item.thumbnail}}" mode="aspectFit" />
                <view class="text-wrap">
                    <view class="title" style="{{item.$loopState__temp5}}">{{item.title}}</view>
                    <view class="desc" style="{{item.$loopState__temp7}}">{{item.remarks || item.title}}</view>
                </view>
            </view>
        </view>
    </view>
</block>