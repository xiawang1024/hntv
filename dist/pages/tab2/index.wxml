<block>
    <view class="list-wrap">
        
        <head title="团队" __triggerObserer="{{ _triggerObserer }}" type="tab"></head>
        <view class="list-box" style="{{anonymousState__temp}}">
            <view class="item" wx:key="{{item.$loopState__temp3}}" bindtap="goToBody"
            wx:for="{{loopArray0}}" wx:for-item="item" data-e-goToBody-so="this" data-e-goToBody-a-a="{{item.thumbnail}}">
                <image class="cover" src="{{item.thumbnail}}" />
            </view>
        </view>
    </view>
</block>